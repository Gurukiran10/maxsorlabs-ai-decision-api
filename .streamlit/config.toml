[client]
# Hides only the "Deploy" button (not relevant here - it targets Streamlit
# Community Cloud) while keeping the rest of the top-right menu (Settings,
# Rerun, About, etc.) intact. "minimal" would hide the whole menu too.
toolbarMode = "viewer"
